# Tomplate with value reference placeholders
# Tests: Section 5.5.2 - Value Reference Placeholders

name: ${project.name}
version: ${project.version:-1.0.0}
description: ${project.description:-A Dart package}

environment:
  sdk: ${deps.sdk}

dependencies:
  path: ${deps.path}
  yaml: ${deps.yaml:-^3.1.0}
  http: ${deps.http}
  # Nested path
  core_version: ${project-info.tom_core.version:-0.0.1}

dev_dependencies:
  test: ${deps-dev.test}
  lints: ${deps-dev.lints}
