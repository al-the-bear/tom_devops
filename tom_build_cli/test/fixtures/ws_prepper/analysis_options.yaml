# This is a test fixture that contains intentionally invalid Dart syntax
# (uses @@@mode markers for WsPrepper testing). Disable analysis.
# Do not include any lints - the lints package is not a dependency.

analyzer:
  exclude:
    - "*.dart"
    - "**/*.dart"
